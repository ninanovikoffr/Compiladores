// Teste semântico completo: válidos e inválidos (incompatíveis)

// Declarações válidas
int $x = 5;
float $y = 3.14;
int $ativo = 1;
float $w = 2.0;

// Casos válidos de expressões aritméticas e unárias
int $somaint = $x + 2;
float $somamix = $x + $y;
float $multmix = $x * $y;
float $divmix = $x / $y;
float $submix = $x - $y;
int $unarioint = -$x;
float $unariofloat = -$y;

// Casos válidos de atribuição e controle
$y = $x;                 // float recebe int (válido)
if ($ativo > 0) {
    print($x);
}

if ($x > 0) {
    print($x);
} else {
    read($y);
}

while ($x != 0) {
    $x = $x - 1;
}

// Casos inválidos de inicialização e atribuição
int $t1 = $y;     // erro: inicialização incompatível (float para int)
$x = $y;                 // erro: atribuição incompatível (float para int)

print("Fim do teste semântico");