// Teste válido para o analisador sintático

int $x = 5;
float $y = 3.14;
bool $ativo = true;

int $soma(int $a, int $b) {
    int $resultado;
    $resultado = $a + $b;
    return $resultado;
}

bool $maiorQueZero(int $n) {
    if ($n > 0)
        return true;
    else
        return false;
}

$x = $soma(2, 3);
$y = $y + 1.5;

if ($x > 3)
    print($x);
else
    print($y);

if ($ativo && $x != 0) {
    print("ativo");
    read($x);
}

while ($x > 0)
    $x = $x - 1;

while ($x < 10) {
    $x = $x + 1;
    print($x);
}

if ($x > 0)
    if ($y > 1.0)
        print($y);
    else
        print($x);

if ($x == 0)
    while ($y > 0.0)
        if ($ativo)
            print("loop");
        else
            read($x);

/* Comentário de
   múltiplas linhas válido */

print("Fim do teste");